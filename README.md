# 🎮 GoldMine — WEBbackendFinal

Backend + Static Frontend для игровой новостной платформы **GoldMine**.  
Проект реализует систему пользователей с ролями и модуль публикации новостей по играм.

Сервер построен на **Node.js + Express**, использует **MongoDB (Mongoose)** для хранения данных и **JWT** для авторизации.

---

# 📌 О проекте

GoldMine — это backend-система для платформы игровых новостей.  
Пользователи могут регистрироваться, входить в систему и просматривать опубликованные новости.  
Авторы и администраторы могут создавать, редактировать и публиковать новости.

Проект демонстрирует:

- REST API архитектуру
- JWT аутентификацию
- Ролевую модель доступа (RBAC)
- Работу с MongoDB
- Middleware авторизации
- Пагинацию и фильтрацию данных
- Разделение backend и frontend

---

# 🧱 Технологический стек

- **Node.js**
- **Express.js**
- **MongoDB**
- **Mongoose**
- **JWT (jsonwebtoken)**
- **bcryptjs**
- **dotenv**
- **nodemon** (для разработки)

---

# 📂 Структура проекта

```
WEBbackendFinal-main/
│
├── Backend/
│   ├── server.js              # Точка входа сервера
│   ├── middleware/
│   │     └── auth.js          # JWT middleware
│   ├── models/
│   │     ├── User.js          # Модель пользователя
│   │     └── News.js          # Модель новости
│   ├── routes/
│   │     ├── users.js         # Роуты пользователей
│   │     └── news.js          # Роуты новостей
│
├── Public/                    # Статический frontend (HTML/CSS/JS)
│
├── .env                       # Переменные окружения (не коммитить)
├── package.json
└── README.md
```

---

# ⚙️ Установка и запуск

## 1️⃣ Клонировать проект

```bash
git clone https://github.com/RuslanSat/WEBbackendFinal.git
cd WEBbackendFinal
```

## 2️⃣ Установить зависимости

```bash
npm install
```

## 3️⃣ Создать файл `.env`

Создайте файл `.env` в корне проекта:

```env
MONGODB_URI=mongodb+srv://username:password@cluster/dbname
JWT_SECRET=your_secret_key
PORT=8080
```

> ⚠️ Не загружайте `.env` в GitHub.

## 4️⃣ Запустить сервер

```bash
node Backend/server.js
```

или (если установлен nodemon):

```bash
npx nodemon Backend/server.js
```

Сервер будет доступен по адресу:

```
http://localhost:8080
```

---

# 🔎 Проверка работы сервера

```http
GET /api/health
```

Ответ:

```json
{
  "status": "Server is running",
  "timestamp": "..."
}
```

---

# 🔐 Аутентификация

Система использует **JWT токены**.

Для защищённых маршрутов необходимо передавать заголовок:

```
Authorization: Bearer <TOKEN>
```

---

# 👥 Ролевая модель

| Роль   | Возможности |
|--------|------------|
| user   | Просмотр опубликованных новостей |
| author | Создание, редактирование и публикация своих новостей |
| admin  | Полный доступ + управление ролями |

---

# 👤 API — Users

## Регистрация

```http
POST /api/users/register
```

```json
{
  "username": "demo",
  "email": "demo@mail.com",
  "password": "123456",
  "role": "user"
}
```

---

## Логин

```http
POST /api/users/login
```

```json
{
  "email": "demo@mail.com",
  "password": "123456"
}
```

---

## Получить профиль

```http
GET /api/users/profile
Authorization: Bearer <TOKEN>
```

---

## Обновить профиль

```http
PUT /api/users/profile
Authorization: Bearer <TOKEN>
```

---

## Изменить роль пользователя (только admin)

```http
PUT /api/users/:id/role
Authorization: Bearer <ADMIN_TOKEN>
```

---

# 📰 API — News

## Получить опубликованные новости

```http
GET /api/news?page=1&limit=10&game=valorant
```

Параметры:
- `page` — номер страницы
- `limit` — количество записей
- `game` — фильтр по игре

---

## Получить свои новости

```http
GET /api/news/mine
Authorization: Bearer <TOKEN>
```

---

## Создать новость

```http
POST /api/news
Authorization: Bearer <TOKEN>
```

```json
{
  "title": "Patch Update",
  "content": "New update released...",
  "game": "Valorant"
}
```

---

## Обновить новость

```http
PUT /api/news/:id
Authorization: Bearer <TOKEN>
```

---

## Удалить новость

```http
DELETE /api/news/:id
Authorization: Bearer <TOKEN>
```

---

## Опубликовать новость

```http
PUT /api/news/:id/publish
Authorization: Bearer <TOKEN>
```

---

## Снять с публикации

```http
PUT /api/news/:id/unpublish
Authorization: Bearer <TOKEN>
```

---

# 🗄 Модели данных

## User

- username (уникальный)
- email (уникальный)
- password (hash через bcrypt)
- role (user | author | admin)
- createdAt
- updatedAt

## News

- title
- content
- game
- author (reference на User)
- publishedAt (null если черновик)
- createdAt
- updatedAt

---

# 🛡 Безопасность

- Хеширование паролей через bcrypt
- JWT авторизация
- Middleware проверки токена
- Проверка прав доступа по роли
- Валидация входных данных

---

# 🧪 Возможные улучшения

- Swagger документация
- Refresh токены
- Загрузка изображений
- Docker конфигурация
- Unit тесты
- Rate limiting
- CI/CD

---

# 👨‍💻 Команда проекта

Добавьте участников команды:

- Backend Developer
- Frontend Developer
- Database Engineer
- QA / Tester

---

# 📄 License

ISC License (согласно package.json)

---

# 📌 Заключение

GoldMine демонстрирует полноценную backend-архитектуру с:

- REST API
- Авторизацией
- Ролевой системой
- Пагинацией
- Работой с базой данных
- Middleware архитектурой

Проект может быть использован как основа для production-уровневой платформы игровых новостей.
